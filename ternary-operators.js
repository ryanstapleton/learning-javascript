const page = 'About';

                    //   conditional  ?  TRUE Block    ELSE  FALSE block
console.log(`class=${ page === 'Home' ? 'master-layout' : 'secondary-layout' }`)

// == -> does not compare type, only value
// === -> compares type and value
